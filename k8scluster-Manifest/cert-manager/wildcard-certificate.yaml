apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-home-noriki-me
  namespace: default
spec:
  secretName: wildcard-home-noriki-me-tls
  issuerRef:
    name: letsencrypt-dns01
    kind: ClusterIssuer
  dnsNames:
    - "*.home.noriki.me"
    - "home.noriki.me"
